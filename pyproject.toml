[project]
name = "google-genai-guide"
version = "0.1.0"
description = "A practical guide to integrating with the Google GenAI ecosystem"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-genai>=2.14.0",
]
